spring.application.name=associationGame

# The app runs behind the Caddy reverse proxy, which terminates TLS and forwards
# X-Forwarded-* headers. Without this Spring thinks every request is plain http.
server.forward-headers-strategy=framework
